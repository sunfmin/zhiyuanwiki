---
import Base from "../../layouts/Base.astro";
import FenduanLookup from "../../components/FenduanLookup.tsx";
import Locator from "../../components/Locator.tsx";
import type { YiFenYiDuan } from "../../lib/fenduan";
import wuli2026 from "../../data/yifenyiduan/wuli-2026.json";

const table = wuli2026 as YiFenYiDuan;
---

<Base
  title="位次定位 · 高考志愿数据 Wiki"
  description="黑龙江输入分数或位次 + 选科，按等效位次给出冲 / 稳 / 保可填报院校专业组。"
>
  <section class="mx-auto max-w-5xl px-4 py-12">
    <h1 class="text-2xl font-bold">位次定位</h1>
    <p class="mt-2 text-slate-600">
      黑龙江 · 2026 —— 输入分数或位次 + 选科，按等效位次给出冲 / 稳 / 保的可填报院校专业组。
    </p>

    <div class="mt-8">
      <Locator client:load wuliTable={table} />
    </div>

    <details class="mt-10">
      <summary class="cursor-pointer text-sm text-slate-500 hover:text-slate-800">
        只想单查 分数 ↔ 位次？
      </summary>
      <div class="mt-3">
        <FenduanLookup client:load table={table} />
      </div>
    </details>

    <p class="mt-8 text-xs text-slate-400">
      分档阈值：你的位次 ≤ 录取线×0.9 → 保；≤ ×1.02 → 稳；≤ ×1.15 → 冲。
      等效位次的跨年总人数缩放待历史一分一段（.xls）接入，当前以位次直接跨年比较。仅供参考，以官方为准。
    </p>
  </section>
</Base>
